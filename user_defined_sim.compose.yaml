services:
  snowglobe:
    build:
      context: .
      dockerfile: Dockerfile
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: ["./start_sim.sh"]
    image: snowglobe:latest
    stdin_open: true # docker run -i
    tty: true # docker run -t
    container_name: snowglobe
    hostname: snowglobe
    healthcheck:
      test: bash -c "[ -f /data/snowglobe/game.id ]"
      timeout: 3s
      retries: 20
    volumes:
      - '${PWD}/config:/home/snowglobe/.config/snowglobe'
      - '${PWD}/logs:/home/snowglobe/logs'
      - '${PWD}/data:/data/snowglobe'
    secrets:
      - openai_key
    # ports:
    #   - 8080:8000

  snowglobe-ui:
    build:
      context: .
      dockerfile: Dockerfile
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
    command: "snowglobe_server -c /config/WoTRM.yaml"
    image: snowglobe:latest
    stdin_open: true # docker run -i
    tty: true # docker run -t
    container_name: snowglobe-ui
    hostname: snowglobe-ui
    volumes:
      - '${PWD}/config:/config'
      - '${PWD}/logs:/home/snowglobe/logs'
      - '${PWD}/data:/data/snowglobe'
    ports:
      - 8080:8000
    depends_on:
      snowglobe:
        condition: service_healthy
secrets:
  openai_key:
    file: ${PWD}/secrets/openaiapi.key
